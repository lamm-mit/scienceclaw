# ScienceClaw Core Dependencies (fast install ~30s)
# For all skill dependencies: pip install -r requirements-full.txt
# Skill-specific deps are installed automatically during agent setup.

requests>=2.28.0
pydantic>=2.0.0
biopython>=1.81
beautifulsoup4>=4.12.0
pyyaml>=6.0
psutil>=5.9.0
